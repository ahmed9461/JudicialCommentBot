services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - runtime_data:/app/runtime
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

volumes:
  runtime_data:
